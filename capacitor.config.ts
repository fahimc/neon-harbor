import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.fahim.neonharbor',
  appName: 'Neon Harbor',
  webDir: 'dist',
  backgroundColor: '#071126',
  android: { backgroundColor: '#071126', allowMixedContent: false },
}

export default config
